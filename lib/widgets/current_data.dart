import 'package:flutter/material.dart';
import 'package:morsoom/model/weather_data.dart';

class CurrentData extends StatelessWidget {
  //final WeatherData weatherData;
  //const CurrentData({Key?key, required this.weatherData}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
    //  child: Text('${weatherData.currentWeather!.temperature}'),
    );
  }
}